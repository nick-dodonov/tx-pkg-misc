load("@tx-kit-ext//rules/build:multi_app.bzl", "multi_app")

multi_app(
    name = "sdl",
    srcs = glob(["*.cpp", "*.h"]),
    data = [".env"],
    deps = [
        "//src/pkg-sdl",
    ],
    droid_manifest = "//demo/try/sdl3-lib:AndroidManifest.xml",
)
