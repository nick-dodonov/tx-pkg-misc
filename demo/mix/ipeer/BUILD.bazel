load("@tx-kit-ext//rules:multi_app.bzl", "multi_app")

multi_app(
    name = "ipeer",
    srcs = glob(["**/*.cpp", "**/*.h"]),
    data = [
        ".env",
    ],
    deps = [
        "//pkg/imgui",
    ],
)
