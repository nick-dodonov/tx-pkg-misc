load("@tx-kit-ext//rules:multi_app.bzl", "multi_app")

multi_app(
    name = "sdl3-1",
    srcs = glob(["*.cpp"]),
    data = [".env"],
    deps = [
        "@tx-pkg-aux//src/pkg-boot",
        "//demo/try/sdl3-lib",
    ],
)
