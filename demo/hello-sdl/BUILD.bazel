load("@tx-kit-ext//rules/build:multi_app.bzl", "multi_app")

multi_app(
    name = "hello-sdl",
    srcs = [
        "main.cpp",
        "FpsCounter.cpp",
        "FpsCounter.h",
    ],
    data = [".env"],
    deps = [
        "//src/pkg-sdl",
    ],
)
