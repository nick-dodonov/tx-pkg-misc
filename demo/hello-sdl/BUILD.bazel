load("@tx-kit-ext//rules/build:tx_binary.bzl", "tx_binary")

tx_binary(
    name = "hello-sdl",
    srcs = glob(["*.cpp"]),
    deps = [
        "@tx-pkg-aux//:pkg-app",
        "@sdl3//:sdl3_static",
    ],
)
