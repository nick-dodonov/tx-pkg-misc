load("@tx-kit-ext//rules/build:tx_binary.bzl", "tx_binary")

tx_binary(
    name = "hello-sdl",
    srcs = [
        "main.cpp",
        "FpsCounter.cpp",
        "FpsCounter.h",
    ],
    data = [".env"],
    deps = [
        "//src/pkg-sdl",
    ],
)
