load("@tx-kit-ext//rules/build:multi_app.bzl", "multi_test")

multi_test(
    name = "misc",
    srcs = glob(["*.cpp", "*.h"]),
    deps = [
        "@googletest//:gtest_main",
        "@tx-pkg-aux//src/pkg-log",
    ],
)
