load("@tx-kit-ext//rules/build:tx_test.bzl", "tx_test")

tx_test(
    name = "misc",
    srcs = glob(["*.cpp", "*.h"]),
    deps = [
        "@googletest//:gtest_main",
        "@tx-pkg-aux//:pkg-log",
    ],
)
